version: 1
from: "infracamp/kickstart-flavor-gaia:testing"

apache_fallback_resource: "/index.php"

config_file:
  template: "config.php.dist"
  target: "config.php"

command:
  build:
    - "composer update"
    - "sudo scripts/install-mail.sh"

  dev:
    - "sudo scripts/startup.sh"

  run:
    - "sudo scripts/startup.sh"

  config:
    - "sudo -E cloudtool.php /opt/etc /etc"
